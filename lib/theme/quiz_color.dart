import 'package:flutter/material.dart';

class QuizColor {
  const QuizColor._();

  static const darkBlue = Color(0xFF152850);
  static const blue = Color(0xFF193469);
  static const lightBlue = Color(0xFF25A8CC);
  static const yellow = Color(0xFFFDD083);
  static const red = Color(0xFFE37373);
  static const green = Color(0xFF81C783);
}
